import React from "react";
import "./style.css";

export default function DescForm({}) {
  const [
    /* create input value state and function to change state */
    inputValue,
    setValue,
  ] = useState(/* Set Default State of Input Value*/ "");

  return {};
}
